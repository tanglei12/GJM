package com.gjp.dao;

import com.gjp.model.ProductRechargeVo;
import com.gjp.util.Pagination;

/**
 * 产品
 */
public interface ProductDao {

    /**
     * 添加充值
     * @param productRechargeVo
     * @return
     */
    int addProductRecharge(ProductRechargeVo productRechargeVo);
    int updateProductRecharge(ProductRechargeVo productRechargeVo);

    Pagination<ProductRechargeVo> queryProductRechargePageList(Pagination<ProductRechargeVo> pagination);

    Pagination<ProductRechargeVo> queryProductRechargeList(Pagination<ProductRechargeVo> pagination);

    ProductRechargeVo queryProductRecharge( ProductRechargeVo productRechargeVo);
}
